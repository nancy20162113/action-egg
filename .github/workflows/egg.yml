name: Test Workflow
on:
  push:
    branches: [main]  # 当 main 分支有推送时触发
jobs:
  build:
    runs-on: ubuntu-latest  # 运行在 Ubuntu 环境
    steps:
      - name: Print Message
        run: echo "Hello from GitHub Actions!"  # 执行打印命令